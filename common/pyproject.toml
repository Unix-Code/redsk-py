[project]
name = "common"
version = "0.1.0"
description = "Common code for client/server applications of redsky-py"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.7,<0.12"]
build-backend = "uv_build"
